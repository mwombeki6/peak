spring:
  main:
    web-application-type: none
  datasource:
    hikari:
      maximum-pool-size: ${PEAK_WORKER_DB_POOL_MAX_SIZE:10}
      minimum-idle: ${PEAK_WORKER_DB_POOL_MIN_IDLE:2}

peak:
  runtime:
    mode: worker
  reliability:
    outbox:
      worker:
        enabled: true
