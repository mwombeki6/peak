spring:
  docker:
    compose:
      enabled: false
  flyway:
    enabled: false
  datasource:
    hikari:
      maximum-pool-size: ${PEAK_DB_POOL_MAX_SIZE:20}
      minimum-idle: ${PEAK_DB_POOL_MIN_IDLE:5}
      connection-timeout: ${PEAK_DB_CONNECTION_TIMEOUT:5s}
      validation-timeout: ${PEAK_DB_VALIDATION_TIMEOUT:2s}
      idle-timeout: ${PEAK_DB_IDLE_TIMEOUT:10m}
      max-lifetime: ${PEAK_DB_MAX_LIFETIME:30m}
  devtools:
    add-properties: false

peak:
  security:
    http:
      jwt:
        enabled: true
      cors:
        allowed-origins: ${PEAK_CORS_ALLOWED_ORIGINS}
    request-context:
      allow-header-identity: false
      allow-trusted-jwt-identity-claims: false
  integrations:
    payment:
      providers:
        vodacom-mpesa:
          base-url: ${PEAK_PAYMENT_VODACOM_MPESA_URL:}
          api-key: ${PEAK_PAYMENT_VODACOM_MPESA_API_KEY:}
          api-secret: ${PEAK_PAYMENT_VODACOM_MPESA_API_SECRET:}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
